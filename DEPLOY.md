# 🚀 Деплой на Replit

## Быстрый деплой через консоль

### Вариант 1: Через npm скрипт (рекомендуется)

```bash
npm run deploy
```

Или с кастомным сообщением коммита:

```bash
./deploy-replit.sh "Ваше сообщение коммита"
```

### Вариант 2: Ручной деплой

```bash
# 1. Добавить изменения
git add -A

# 2. Создать коммит
git commit -m "Ваше сообщение"

# 3. Запушить на GitHub
git push origin main
```

## Настройка автоматического деплоя в Replit

Чтобы Replit автоматически деплоил изменения из GitHub:

1. Откройте ваш Repl на [replit.com](https://replit.com)
2. Нажмите кнопку **"Deploy"** в левой панели
3. Выберите **"Autoscale"** (уже настроено в `.replit`)
4. В разделе **"Source"** выберите **"GitHub"**
5. Подключите репозиторий и выберите ветку `main`
6. Включите опцию **"Auto-deploy on push"**

После этого каждый `git push` будет автоматически деплоить на Replit! 🎉

## Альтернатива: Pull из GitHub в Replit

Если автодеплой не настроен, после `git push` нужно:

1. Зайти в Replit
2. Открыть ваш Repl
3. Нажать **"Pull from GitHub"** в меню Git
4. Replit автоматически пересоберёт и перезапустит приложение

## Проверка деплоя

После деплоя проверьте:

- ✅ Приложение запустилось без ошибок
- ✅ Новые изменения видны на сайте
- ✅ База данных работает корректно

## Troubleshooting

### Если деплой не сработал:

1. Проверьте логи в Replit Console
2. Убедитесь, что все зависимости установлены
3. Проверьте, что `.replit` конфигурация корректна
4. Попробуйте вручную запустить `npm run build` в Replit Shell

### Если нужно откатить изменения:

```bash
# Откатить последний коммит локально
git reset HEAD~1

# Или откатить на конкретный коммит
git reset --hard <commit-hash>

# Форс-пуш (осторожно!)
git push origin main --force
```

## Полезные команды

```bash
# Просмотр статуса
git status

# Просмотр истории
git log --oneline -10

# Просмотр изменений
git diff

# Отменить все локальные изменения
git reset --hard origin/main
```
