# 🧪 Как протестировать систему аналитики

## Вариант 1: Быстрая проверка (без установки)

Проверьте, что все закоммичено:

```bash
git status
# Должно показать "nothing to commit, working tree clean"

git log --oneline -3
# Должно показать 3 последних коммита с аналитикой
```

## Вариант 2: Проверка структуры файлов

```bash
# Проверьте, что созданы все файлы
ls -la analytics/

# Должно быть:
# - README.md (основная документация)
# - QUICKSTART.md (быстрый старт)
# - TEST.md (этот файл)
# - migrations/ (4 SQL файла)
# - queries/ (2 SQL файла с запросами)
# - integration/ (документация A/B)
# - examples/ (примеры React компонентов)

# Проверьте backend и frontend
ls -la server/analytics.ts
ls -la client/src/lib/analytics.ts
ls -la client/src/lib/analytics-ab-helpers.ts
```

## Вариант 3: Установка и тестирование в БД

### Шаг 1: Получите DATABASE_URL

Если у вас проект на Replit или есть Neon:

```bash
# Найдите DATABASE_URL в настройках проекта или .env
echo $DATABASE_URL
```

### Шаг 2: Запустите тестовый скрипт

Этот скрипт проверит, всё ли установлено (БЕЗ изменения данных):

```bash
psql $DATABASE_URL -f analytics/test_analytics.sql
```

**Что он проверяет:**
- ✅ Существуют ли таблицы (7 штук)
- ✅ Существуют ли ETL функции (6 штук)
- ✅ Существуют ли BI views (8 штук)
- ✅ Установлен ли pg_cron
- ✅ Сколько данных в каждой таблице

**Ожидаемый результат:**
```
=== ИТОГОВЫЙ СТАТУС ===
┌─────────────────────────────────────────┐
│  СТАТУС КОМПОНЕНТОВ СИСТЕМЫ АНАЛИТИКИ  │
├─────────────────────────────────────────┤
│ Таблицы (7 шт):          ✓ ОК      │
│ ETL функции (6 шт):      ✓ ОК      │
│ BI Views (8 шт):         ✓ ОК      │
│ pg_cron:                 ✓ ОК      │
└─────────────────────────────────────────┘

🎉 Все компоненты установлены! Система готова к работе.
```

### Шаг 3 (опционально): Создайте демо данные

Этот скрипт создаст тестовые события и обработает их:

```bash
psql $DATABASE_URL -f analytics/demo_data.sql
```

**Что он делает:**
1. Создаёт 5 тестовых сессий с событиями
2. Запускает ETL обработку
3. Показывает результаты (воронку, A/B тест, статистику)

**Удаление демо данных:**
```sql
DELETE FROM raw_events WHERE session_id LIKE 'sess_test_%';
```

## Вариант 4: Тестирование через браузер

### Шаг 1: Добавьте инициализацию

Откройте `client/src/main.tsx` и добавьте в конец:

```typescript
// Временно для теста
import { initAnalytics } from "./lib/analytics";
initAnalytics();
console.log("✅ Analytics initialized");
```

### Шаг 2: Запустите приложение

```bash
npm run dev
```

### Шаг 3: Откройте браузер и консоль

1. Откройте http://localhost:5000
2. Откройте DevTools (F12) → Console
3. Вы должны увидеть: `✅ Analytics initialized`

### Шаг 4: Проверьте отправку событий

В консоли браузера выполните:

```javascript
// Импортируем функцию (если у вас уже открыта страница)
// или просто откройте любую страницу - событие page_view отправится автоматически

// Проверьте в Network tab (F12 → Network):
// Должен появиться запрос POST /api/analytics/log
```

### Шаг 5: Проверьте в базе

```sql
-- Посмотрите последние события
SELECT 
  event_name,
  source,
  page,
  event_time
FROM raw_events
ORDER BY event_time DESC
LIMIT 5;

-- Должно показать события, включая page_view
```

## Вариант 5: Тестирование A/B интеграции

### Проверьте существующие эксперименты:

```sql
SELECT 
  test_id,
  name,
  status,
  variants
FROM experiments
WHERE status = 'active';
```

### Создайте тестовый эксперимент:

1. Откройте админку: http://localhost:5000/admin
2. Перейдите в раздел "Experiments"
3. Создайте эксперимент:
   - test_id: `test-button-color`
   - name: `Тест цвета кнопки`
   - variants:
   ```json
   [
     {"id": "control", "name": "Синий", "weight": 50, "config": {"color": "blue"}},
     {"id": "red", "name": "Красный", "weight": 50, "config": {"color": "red"}}
   ]
   ```

### Проверьте назначение варианта:

В консоли браузера:

```javascript
// Получите текущий вариант
const variant = useAbTesting().getTestVariant('test-button-color');
console.log('Assigned variant:', variant);
```

### Отправьте событие с A/B данными:

```javascript
// В консоли браузера
import { trackEvent } from './lib/analytics';

trackEvent('test_button_clicked', {
  experimentKey: 'test-button-color',
  experimentVariant: 'control', // или 'red'
  properties: { button_position: 'header' }
});
```

### Проверьте в базе:

```sql
SELECT 
  event_name,
  experiment_key,
  experiment_variant,
  properties
FROM raw_events
WHERE experiment_key = 'test-button-color'
ORDER BY event_time DESC;
```

## Проверка pg_cron (автоматический ETL)

```sql
-- Список всех задач
SELECT 
  jobname,
  schedule,
  active
FROM cron.job
ORDER BY jobname;

-- История выполнения (последние 10)
SELECT 
  jobname,
  status,
  start_time,
  end_time - start_time AS duration
FROM cron.job_run_details jrd
JOIN cron.job j ON j.jobid = jrd.jobid
ORDER BY start_time DESC
LIMIT 10;

-- Должно показать выполнение каждые 5 минут
```

## Проверка BI Views

```sql
-- Посмотрите доступные view
SELECT table_name 
FROM information_schema.views 
WHERE table_name LIKE 'v_analytics_%'
ORDER BY table_name;

-- Попробуйте запросить данные
SELECT * FROM v_analytics_events LIMIT 5;
SELECT * FROM v_analytics_sessions LIMIT 5;
SELECT * FROM v_analytics_daily_stats ORDER BY "Дата" DESC LIMIT 7;
```

## Что делать, если что-то не работает?

### Таблицы не созданы

```bash
# Запустите миграции по порядку
psql $DATABASE_URL -f analytics/migrations/001_analytics_tables.sql
psql $DATABASE_URL -f analytics/migrations/002_etl_functions.sql
psql $DATABASE_URL -f analytics/migrations/003_pg_cron_setup.sql
psql $DATABASE_URL -f analytics/migrations/004_bi_views.sql
```

### pg_cron не установлен

```sql
CREATE EXTENSION IF NOT EXISTS pg_cron;
```

Затем заново запустите:
```bash
psql $DATABASE_URL -f analytics/migrations/003_pg_cron_setup.sql
```

### События не отправляются с фронтенда

1. Проверьте консоль браузера на ошибки
2. Проверьте Network tab - есть ли запросы к `/api/analytics/log`
3. Проверьте, что `initAnalytics()` вызван в `main.tsx`

### ETL не обрабатывает данные

```sql
-- Запустите ETL вручную
SELECT process_sessions();
SELECT process_events_clean();

-- Проверьте логи
SELECT * FROM etl_runs ORDER BY start_time DESC LIMIT 5;
```

## Быстрый чеклист

- [ ] Коммиты созданы (3 штуки)
- [ ] Файлы существуют (13 файлов в analytics/)
- [ ] SQL миграции запущены (4 файла)
- [ ] Тестовый скрипт показывает ✓ ОК для всех компонентов
- [ ] Демо данные созданы и обработаны
- [ ] Приложение запущено и `initAnalytics()` вызван
- [ ] События отправляются в `raw_events`
- [ ] pg_cron задачи активны
- [ ] BI views доступны для запросов

## Что дальше?

После успешного тестирования:

1. **Прочитайте** `analytics/QUICKSTART.md` для продакшен настройки
2. **Изучите** примеры в `analytics/examples/react-components.tsx`
3. **Подключите** DataLens к вашей БД
4. **Создайте** первый реальный A/B эксперимент
5. **Начните** отслеживать реальные события в компонентах

---

**Нужна помощь?** Читайте полную документацию в `analytics/README.md`
