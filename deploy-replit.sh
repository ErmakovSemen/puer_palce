#!/bin/bash

# Скрипт для деплоя на Replit через git push
# Использование: ./deploy-replit.sh "commit message"

set -e  # Остановить при ошибке

echo "🚀 Деплой на Replit..."

# Проверка наличия изменений
if [[ -n $(git status -s) ]]; then
  echo "📝 Есть незакоммиченные изменения"
  
  # Добавить все изменения
  git add -A
  
  # Создать коммит
  COMMIT_MSG="${1:-Автоматический деплой $(date +'%Y-%m-%d %H:%M:%S')}"
  git commit -m "$COMMIT_MSG"
  echo "✅ Коммит создан: $COMMIT_MSG"
else
  echo "✨ Нет изменений для коммита"
fi

# Запушить на origin
echo "📤 Пуш на GitHub..."
git push origin main

echo ""
echo "✅ Деплой завершён!"
echo ""
echo "Теперь зайдите в Replit и нажмите 'Pull from GitHub'"
echo "Или настройте автоматический деплой через Replit Deployments"
echo ""
echo "Альтернатива: используйте Replit Deployments в UI:"
echo "1. Откройте ваш Repl"
echo "2. Нажмите Deploy в левой панели"
echo "3. Настройте автодеплой из GitHub"
